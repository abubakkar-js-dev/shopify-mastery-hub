"use client";
import { LessonViewShell, LessonViewShellProps } from "../features/lessons/components/LessonViewShell";

export default function LessonView(props: LessonViewShellProps) {
  return <LessonViewShell {...props} />;
}
